#!/usr/bin/env bash

set -euo pipefail

todo="${1:-todo-list.txt}"

while IFS= read -r line || [[ -n "$line" ]]; do
  [[ -z "$line" ]] && continue
  echo "Running: $line"
  eval "$line"
done < "$todo"
